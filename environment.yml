name: geoglows_ecflow
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - ecflow
  - nco
  - pyyaml
  - numpy
  - numcodecs
  - dask
  - pandas
  - fastparquet
  - xarray
  - netcdf4
  - zarr
  - boto3
  - requests
  - pip:
    - basininflow>=0.14.0
    - river-route>=2.1.1
