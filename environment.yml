name: geoglows_ecflow
channels:
  - conda-forge
  - defaults
dependencies:
  - ecflow
  - nco
  - pyyaml
  - numpy
  - dask
  - pandas
  - fastparquet
  - xarray
  - netcdf4
  - zarr
  - boto3
  - requests
  - pip:
    - basininflow @ git+https://pypi.org/project/basininflow/
